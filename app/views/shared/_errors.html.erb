<% if object.errors.any? %>
  <div>
    <%= pluralize(object.errors.count, "error") %> prevented this movie from being submitted:
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>